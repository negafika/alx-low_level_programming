#include "main.h"

/**
 * _strcmp - a function that compares strings.
 * @s1: string
 * @s2: string2
 * 
 * Return: int
 */

int _strcmp(char *s1, char *s2)
{
int i;

while (s1[s1_len] != '\0')
	s1_len++;
while (s2[s2_len] != '\0')
	s2_len++;
if (s1_len == s2_len)
	return (0)
else if (s1_len > s2_len)
	return (15)

return (-15);
}
