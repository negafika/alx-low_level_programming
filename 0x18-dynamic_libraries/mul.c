int mul(int, int);
/**
 * mul - adds two numbers together.
 * @i: The first number to be added.
 * @j: The second number to be added.
 * Return: the sum of two numbers.
 */
int mul(int i, int j)
{
	return (i * j);
}
